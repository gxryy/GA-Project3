import React from "react";
import { Typography } from "@mui/material";

const FlightStop = ({ flightStops }) => {
  console.log(flightStops);

  return (
    <div>
      <Typography>heyooo</Typography>
    </div>
  );
};

export default FlightStop;
